generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder model to allow Prisma Client generation
model User {
  id    String @id @default(uuid())
  email String @unique
}
